import styled from 'styled-components';

const AuthHeaderS = styled.header`
  align-items: center;
  display: flex;
  flex-direction: column;
  justify-content: center;

  .logo {
    width: 40pt;
    height: 40pt;
  }

  h4 {
    margin: 16px 0 0;
  }
`;

export default AuthHeaderS;
